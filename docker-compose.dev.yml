services:
  vaca-app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    container_name: hci-vaca-app-dev
    environment:
      - NODE_ENV=development
